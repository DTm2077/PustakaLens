name: pusakalens
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0' # Adjusted to a more standard Dart 3 range

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Required for Local Database/State
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.1.2

  # Essential for API and Image Upload
  http: ^1.2.0 
  image_picker: ^1.0.7 # For XFile support (Mobile/Web)

  # UI Libraries
  google_fonts: ^6.2.1
  ionicons: ^0.2.2
  shared_preferences: ^2.5.3

  # REMOVED FIREBASE DEPENDENCIES

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  # hive_generator: ^2.0.1  # If you use Hive code generation
  # build_runner: ^2.4.9    # If you use code generation

flutter:
  uses-material-design: true
  
  # CORRECTED ASSET PATHS
  assets:
    # Ensure these folders exist inside your project's 'assets' directory
    - assets/images/batik/
    - assets/images/patung/ 
    - assets/images/wayang/
    - assets/images/logo/
    # Note: I removed the trailing 'Q' and standardized the path structure.